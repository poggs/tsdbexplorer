<% content_for :jumbotron do %>

<header class="jumbotron masthead">
 <div class="inner">
  <div class="container">
   <%= render :partial => 'common/flash', :locals => { :flash => flash } %>
   <h1>Open Train Times</h1>
   <p class="lead">Train information using Open Data</p>

   <%= render :partial => 'common/autocomplete_javascript', :locals => { :element => 'term', :controller => 'location', :action => 'search', :min_length => 3 } %>
   <form class="form-search" style="text-align: center;" action="<%= url_for :controller => 'location', :action => 'search' %>">
    <p class="example">
     Show me trains at <input class="span3" id="term" name="term" type="text">
     <select name="date" class="span3">
      <% @dates.each do |d| %>
       <%= content_tag :option, { :value => d[:yyyymmdd], :selected => d[:selected] } do %><%= d[:human] %><%= @date %><% end %>
      <% end %>
     </select>
     at
     <input class="span1" id="time" name="time" size="span1" type="text"/>
     <button type="submit" class="btn btn-primary"><i class="icon-search icon-white"></i> Search</button>
    </p>

  </div>
 </div>
</header>

<% end %>

<div class="row">
 <div class="span4">
  <h2>What?</h2>
  <p>We're using Open Data to bring you train information.  Right now, we have timetables, and we're working on real-time running information and historical data.</p>
 </div>
 <div class="span4">
  <h2>Why?</h2>
  <p>To help you make even better travel decisions!  We can't make your train always run on time, but we promise to be as clear and honest as is possible.</p>
 </div>
 <div class="span4">
  <h2>Who?</h2>
  <p>This site is designed, built and maintained by <a href="http://blog.poggs.com/">Peter Hicks</a>.  The <a href="http://github.com/poggs/tsdbexplorer/">source code is available</a> on <a href="http://www.github.com/">GitHub</a> and you're free to use it for non-commercial purposes.</p>
 </div>
</div>
