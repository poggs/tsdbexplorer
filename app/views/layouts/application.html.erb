<!DOCTYPE html>
<html>
 <head>
  <title>Open Train Times<% if Rails.env != 'production' %> (<%= Rails.env.titlecase %>)<% end %></title>
  <%= stylesheet_link_tag :all %>
  <%= yield(:head) %>
  <%= javascript_include_tag "tsdbexplorer", "jquery-1.6.1.min", "jquery-ui-1.8.12.custom.min", "jquery.twisty" %>
  <%= csrf_meta_tag %>
  <%= yield(:script) %>
  <link rel="shortcut icon" href="<%= url_for 'images/favicon.png' %>" />
 </head>
 <body>

  <div id="header">
   <div>Open Train Times<% if Rails.env != 'production' %> (<%= Rails.env.titlecase %>)<% end %></div>
  </div>

  <div id="page">

   <div id="body"> 
    <% if flash[:warn] %>
     <div class="flash warn"><%= flash[:warn] %></div>
    <% end %>
    <%= yield %>
   </div>

   <div id="footer">
    <a href="http://github.com/poggs/tsdbexplorer">Open Train Times</a> is &copy;2011 <a href="mailto:peter.hicks@poggs.co.uk">Peter Hicks</a>. Data provided by <a href="http://www.networkrail.co.uk/">Network Rail</a>.  Please read the important information in the <a href="disclaimer">disclaimer</a>.
    <span style="float: right;">
     <% if advanced_mode? %>
      <%= link_to (image_tag 'modes/normal.png', :alt => 'Change to Normal Mode'), :controller => 'main', :action => 'toggle_mode' %>
     <% else %>
      <%= link_to (image_tag 'modes/advanced.png', :alt => 'Change to Advanced Mode'), :controller => 'main', :action => 'toggle_mode' %>
     <% end %>
    </span>
   </div>
  </div>

 </body>
</html>
